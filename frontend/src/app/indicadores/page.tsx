import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function IndicadoresPage() {
  return (
    <PlaceholderPage
      title="Indicadores"
      contract="GET /indicadores e /indicadores/:id/historico. Estado atual apenas (meta.serie_temporal=false). Card ou tabela, sem grafico temporal."
    />
  );
}

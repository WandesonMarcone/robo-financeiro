import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function PreferenciasPage() {
  return (
    <PlaceholderPage
      title="Preferencias"
      contract="GET/PATCH /preferencias e POST /preferencias/restaurar. Flags web/telegram e frequencia. Gating 10.3 permanece no backend."
    />
  );
}
